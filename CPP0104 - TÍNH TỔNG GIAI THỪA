#include<iostream>
#include<iomanip>
using namespace std;
long long int test(int n) {
	long long int sum=1,s=0;
	for (int i = 1; i <= n; i++) {
		sum *= i;
		s += sum;
	}
	return s;
}
int main() {
		int n; cin >> n;
		cout << test(n) << endl;
}
