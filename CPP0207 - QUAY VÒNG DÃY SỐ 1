#include<iostream>
using namespace std;

int main() {
	int t; cin >> t;
	while (t--) {
		int n, d;
		cin >> n >> d;
		int M[1000000];
		for (int i = 0; i < n; i++)cin >> M[i];
		for (int i = d; i < n; i++)cout << M[i] << " ";
		for (int i = 0; i < d; i++)cout << M[i] << " ";
		cout << endl;
	}
}
