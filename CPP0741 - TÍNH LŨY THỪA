#include<iostream> 
using namespace std ;
typedef long long int ll;
int main(){
	int t; cin >> t;
	while (t--) {
		ll x, y, p; cin >> x >> y >> p;
		ll sum = 1;
		while (y--)sum = (sum * x) % p;
		cout << sum << endl;
	}
}
